spring:
  application:
    name: product-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: "config-server"

eureka:
  client:
    service-url:
      defaultZone: http://localhost:19090/eureka/
